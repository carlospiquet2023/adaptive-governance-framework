{
  "name": "adaptive-governance-framework",
  "version": "1.0.0",
  "description": "Framework enterprise para governança adaptativa de software, com núcleo TypeScript, UI React, integrações externas e pronto para produção.",
  "scripts": {
    "dev": "docker-compose -f docker-compose.yml -f docker-compose.override.yml up --build",
    "prod": "docker-compose -f docker-compose.prod.yml up -d --build",
    "test": "docker-compose -f docker-compose.test.yml up --build",
    "lint": "docker-compose -f docker-compose.linter.yml up --build",
    "format": "npm run format:core && npm run format:ui",
    "format:core": "cd core && npm run format",
    "format:ui": "cd ui && npm run format"
  },
  "author": "",
  "license": "MIT"
}
